#include <vector>
#include <array>
#include <map>
#include <string>
#include <optional> // C++17

// A C++ szabványkönyvtár alap építőköve a sablon.
// A neve is tükrözi ezt STL = Standard Template Library

int main() {
	std::vector<int> numbers;
	std::array<std::string, 5> names;
	std::map<int, float> int_to_float_map;
	std::optional<double> result;
	return 0;
}
